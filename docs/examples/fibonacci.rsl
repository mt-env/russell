fn fibonacci(x: Int) -> Int {
    return if x == 0 then
        0
    else if x == 1 then
        1
    else
        fibonacci(x - 1) + fibonacci(x - 2);
}

fn main() -> Int {
    read Int num;
    echo Int fibonacci(num);
    return 0;
}
